<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Aayan Jakkani — Portfolio</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;0,900;1,400&family=DM+Mono:wght@300;400;500&family=Cormorant+Garamond:ital,wght@0,300;0,400;1,300&display=swap" rel="stylesheet">
<style>
  :root {
    --ink: #0d0d0d;
    --paper: #f5f0e8;
    --cream: #ede8db;
    --accent: #c8442a;
    --gold: #b8973a;
    --muted: #7a7368;
    --border: #cec7b5;
  }
  *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
  html { scroll-behavior: smooth; }
  body {
    background: var(--paper);
    color: var(--ink);
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px;
    line-height: 1.7;
    overflow-x: hidden;
    cursor: none;
  }
  .cursor {
    width: 8px; height: 8px;
    background: var(--accent);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9999;
    transform: translate(-50%, -50%);
    transition: transform 0.1s ease;
  }
  .cursor-ring {
    width: 36px; height: 36px;
    border: 1px solid var(--accent);
    border-radius: 50%;
    position: fixed;
    pointer-events: none;
    z-index: 9998;
    transform: translate(-50%, -50%);
    transition: all 0.25s ease;
    opacity: 0.6;
  }
  body::before {
    content: '';
    position: fixed;
    inset: 0;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)' opacity='0.04'/%3E%3C/svg%3E");
    pointer-events: none;
    z-index: 1000;
    opacity: 0.4;
  }
  nav {
    position: fixed; top: 0; left: 0; right: 0;
    display: flex; align-items: center; justify-content: space-between;
    padding: 1.5rem 4rem;
    background: rgba(245,240,232,0.85);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
    z-index: 500;
  }
  .nav-logo {
    font-family: 'Playfair Display', serif;
    font-weight: 900;
    font-size: 1.3rem;
    letter-spacing: -0.02em;
    color: var(--ink);
    text-decoration: none;
  }
  .nav-links { display: flex; gap: 2.5rem; list-style: none; }
  .nav-links a {
    font-family: 'DM Mono', monospace;
    font-size: 0.72rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--muted);
    text-decoration: none;
    transition: color 0.2s;
  }
  .nav-links a:hover { color: var(--accent); }
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    position: relative;
    overflow: hidden;
  }
  .hero-left {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 8rem 4rem 6rem;
    position: relative;
  }
  .hero-right {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8rem 4rem 6rem;
    background: var(--cream);
    position: relative;
  }
  .hero-right::before {
    content: '';
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(0deg, transparent, transparent 39px, var(--border) 39px, var(--border) 40px);
    opacity: 0.4;
  }
  .hero-number {
    font-family: 'DM Mono', monospace;
    font-size: 0.7rem;
    letter-spacing: 0.2em;
    color: var(--muted);
    margin-bottom: 1.5rem;
    opacity: 0;
    animation: fadeUp 0.8s ease 0.2s forwards;
  }
  .hero-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(4rem, 8vw, 7rem);
    font-weight: 900;
    line-height: 0.92;
    letter-spacing: -0.03em;
    margin-bottom: 2rem;
    opacity: 0;
    animation: fadeUp 0.9s ease 0.35s forwards;
  }
  .hero-title em { font-style: italic; font-weight: 400; color: var(--accent); }
  .hero-desc {
    font-size: 1.15rem;
    color: var(--muted);
    max-width: 380px;
    font-weight: 300;
    opacity: 0;
    animation: fadeUp 0.9s ease 0.5s forwards;
  }
  .hero-scroll {
    position: absolute;
    bottom: 3rem; left: 4rem;
    display: flex; align-items: center; gap: 1rem;
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--muted);
    opacity: 0;
    animation: fadeUp 1s ease 0.9s forwards;
  }
  .hero-scroll::before { content: ''; width: 40px; height: 1px; background: var(--muted); }
  .hero-card {
    position: relative; z-index: 1;
    background: var(--ink);
    color: var(--paper);
    padding: 3rem;
    width: 340px;
    opacity: 0;
    animation: fadeUp 1s ease 0.6s forwards;
  }
  .hero-card .card-label {
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.2em;
    color: var(--gold);
    text-transform: uppercase;
    margin-bottom: 2rem;
  }
  .hero-card .card-name { font-family: 'Playfair Display', serif; font-size: 1.8rem; font-weight: 700; margin-bottom: 0.5rem; }
  .hero-card .card-role { font-size: 0.95rem; color: rgba(245,240,232,0.6); font-style: italic; margin-bottom: 2rem; }
  .hero-card .card-divider { width: 40px; height: 1px; background: var(--gold); margin-bottom: 2rem; }
  .hero-card .card-stat { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: 1rem; font-size: 0.9rem; }
  .hero-card .stat-num { font-family: 'Playfair Display', serif; font-size: 1.8rem; font-weight: 700; color: var(--gold); }
  .hero-card .stat-label { font-family: 'DM Mono', monospace; font-size: 0.65rem; letter-spacing: 0.1em; color: rgba(245,240,232,0.5); }
  section { padding: 8rem 4rem; }
  .section-eyebrow {
    font-family: 'DM Mono', monospace;
    font-size: 0.65rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--accent);
    margin-bottom: 1rem;
    display: flex; align-items: center; gap: 1rem;
  }
  .section-eyebrow::after { content: ''; flex: 0 0 60px; height: 1px; background: var(--accent); }
  .section-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.5rem, 4vw, 3.8rem);
    font-weight: 900;
    line-height: 1.05;
    letter-spacing: -0.02em;
    margin-bottom: 4rem;
  }
  .section-title em { font-style: italic; font-weight: 400; }
  .about {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 8rem;
    align-items: center;
    background: var(--cream);
    border-top: 1px solid var(--border);
    border-bottom: 1px solid var(--border);
  }
  .about-text p { font-size: 1.15rem; font-weight: 300; color: var(--muted); margin-bottom: 1.5rem; }
  .about-text p strong { color: var(--ink); font-weight: 600; }
  .about-right { display: flex; flex-direction: column; gap: 2rem; }
  .about-fact {
    padding: 2rem;
    border: 1px solid var(--border);
    background: var(--paper);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  .about-fact:hover { transform: translateX(8px); box-shadow: -8px 0 0 var(--accent); }
  .about-fact-num { font-family: 'Playfair Display', serif; font-size: 3.5rem; font-weight: 900; color: var(--accent); line-height: 1; margin-bottom: 0.25rem; }
  .about-fact-label { font-family: 'DM Mono', monospace; font-size: 0.7rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); }
  .skills { border-bottom: 1px solid var(--border); }
  .skills-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 2px; }
  .skill-block {
    padding: 3rem 2.5rem;
    background: var(--cream);
    transition: background 0.3s, color 0.3s;
    position: relative;
    overflow: hidden;
  }
  .skill-block::before { content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 3px; background: var(--accent); transition: width 0.4s ease; }
  .skill-block:hover { background: var(--ink); color: var(--paper); }
  .skill-block:hover::before { width: 100%; }
  .skill-block:hover .skill-icon { color: var(--gold); }
  .skill-block:hover .skill-cat { color: rgba(245,240,232,0.4); }
  .skill-icon { font-size: 2rem; margin-bottom: 1.5rem; color: var(--accent); transition: color 0.3s; }
  .skill-name { font-family: 'Playfair Display', serif; font-size: 1.4rem; font-weight: 700; margin-bottom: 0.75rem; }
  .skill-cat { font-family: 'DM Mono', monospace; font-size: 0.7rem; letter-spacing: 0.12em; text-transform: uppercase; color: var(--muted); transition: color 0.3s; margin-bottom: 1.5rem; }
  .skill-tags { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .skill-tag { font-family: 'DM Mono', monospace; font-size: 0.65rem; padding: 0.2rem 0.6rem; border: 1px solid currentColor; opacity: 0.6; letter-spacing: 0.08em; }
  .projects { background: var(--ink); color: var(--paper); }
  .projects .section-eyebrow { color: var(--gold); }
  .projects .section-eyebrow::after { background: var(--gold); }
  .project-featured {
    margin-bottom: 2px;
    background: #0a0905;
    border: 1px solid rgba(184,151,58,0.25);
    padding: 4rem;
    position: relative;
    overflow: hidden;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  .project-featured::before {
    content: '';
    position: absolute;
    top: -80px; right: -80px;
    width: 400px; height: 400px;
    background: radial-gradient(circle, rgba(184,151,58,0.06) 0%, transparent 70%);
    pointer-events: none;
  }
  .project-featured-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: 'DM Mono', monospace;
    font-size: 0.62rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--gold);
    border: 1px solid rgba(184,151,58,0.4);
    padding: 0.35rem 0.9rem;
    margin-bottom: 1.5rem;
  }
  .project-featured-badge::before {
    content: '';
    width: 6px; height: 6px;
    border-radius: 50%;
    background: var(--gold);
    animation: pulse 2s infinite;
  }
  @keyframes pulse {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.7); }
  }
  .project-featured-title { font-family: 'Playfair Display', serif; font-size: clamp(2.2rem, 3.5vw, 3.2rem); font-weight: 900; line-height: 1.05; letter-spacing: -0.02em; margin-bottom: 0.5rem; }
  .project-featured-subtitle { font-family: 'DM Mono', monospace; font-size: 0.7rem; letter-spacing: 0.15em; color: rgba(184,151,58,0.7); text-transform: uppercase; margin-bottom: 1.5rem; }
  .project-featured-desc { font-size: 1rem; color: rgba(245,240,232,0.6); font-weight: 300; line-height: 1.75; margin-bottom: 2.5rem; }
  .project-featured-stack { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 2.5rem; }
  .project-featured-pill { font-family: 'DM Mono', monospace; font-size: 0.62rem; padding: 0.3rem 0.8rem; background: rgba(184,151,58,0.1); border: 1px solid rgba(184,151,58,0.3); color: var(--gold); letter-spacing: 0.08em; }
  .project-featured-right { display: flex; flex-direction: column; gap: 1px; }
  .feat-module { background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.06); padding: 1.4rem 1.8rem; display: flex; align-items: flex-start; gap: 1.2rem; transition: background 0.25s; }
  .feat-module:hover { background: rgba(184,151,58,0.07); }
  .feat-module-icon { font-size: 1.2rem; flex-shrink: 0; margin-top: 0.1rem; }
  .feat-module-name { font-family: 'Playfair Display', serif; font-size: 1rem; font-weight: 700; margin-bottom: 0.2rem; }
  .feat-module-desc { font-family: 'DM Mono', monospace; font-size: 0.62rem; color: rgba(245,240,232,0.4); letter-spacing: 0.06em; line-height: 1.6; }
  .project-featured-num { position: absolute; bottom: 2.5rem; right: 3rem; font-family: 'Playfair Display', serif; font-size: 8rem; font-weight: 900; color: rgba(255,255,255,0.025); line-height: 1; pointer-events: none; user-select: none; }
  .projects-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 2px; }
  .project-card { padding: 3.5rem; background: #161512; position: relative; overflow: hidden; transition: background 0.3s; }
  .project-card:hover { background: #1e1b17; }
  .project-card::after { content: attr(data-num); position: absolute; top: 2rem; right: 2.5rem; font-family: 'Playfair Display', serif; font-size: 5rem; font-weight: 900; color: rgba(255,255,255,0.04); line-height: 1; }
  .project-tag { font-family: 'DM Mono', monospace; font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--gold); margin-bottom: 1.5rem; }
  .project-title { font-family: 'Playfair Display', serif; font-size: 1.8rem; font-weight: 700; margin-bottom: 1rem; line-height: 1.15; }
  .project-desc { font-size: 0.95rem; color: rgba(245,240,232,0.55); font-weight: 300; margin-bottom: 2rem; line-height: 1.7; }
  .project-stack { display: flex; flex-wrap: wrap; gap: 0.5rem; }
  .project-pill { font-family: 'DM Mono', monospace; font-size: 0.62rem; padding: 0.25rem 0.75rem; background: rgba(184,151,58,0.12); border: 1px solid rgba(184,151,58,0.3); color: var(--gold); letter-spacing: 0.08em; }
  .certifications { background: var(--paper); border-bottom: 1px solid var(--border); }
  .cert-list { display: flex; flex-direction: column; gap: 0; }
  .cert-item { display: grid; grid-template-columns: 80px 1fr 200px; align-items: center; padding: 2rem 0; border-bottom: 1px solid var(--border); gap: 3rem; transition: padding 0.3s; }
  .cert-item:hover { padding-left: 1.5rem; }
  .cert-item:hover .cert-num { color: var(--accent); }
  .cert-num { font-family: 'Playfair Display', serif; font-size: 3rem; font-weight: 900; color: var(--border); line-height: 1; transition: color 0.3s; }
  .cert-name { font-family: 'Playfair Display', serif; font-size: 1.4rem; font-weight: 700; margin-bottom: 0.25rem; }
  .cert-issuer { font-family: 'DM Mono', monospace; font-size: 0.68rem; letter-spacing: 0.15em; text-transform: uppercase; color: var(--muted); }
  .cert-badge { justify-self: end; font-family: 'DM Mono', monospace; font-size: 0.65rem; padding: 0.4rem 1rem; border: 1px solid var(--border); letter-spacing: 0.1em; color: var(--muted); text-transform: uppercase; }
  .internship { background: var(--cream); border-bottom: 1px solid var(--border); display: grid; grid-template-columns: 1fr 1fr; gap: 6rem; align-items: center; }
  .internship-visual { position: relative; height: 500px; display: flex; align-items: center; justify-content: center; }
  .internship-card-bg { position: absolute; width: 340px; height: 420px; background: var(--ink); transform: rotate(-3deg); top: 20px; left: 20px; }
  .internship-card-main { position: relative; z-index: 2; width: 340px; background: var(--paper); border: 1px solid var(--border); padding: 3rem; }
  .internship-card-main .company-logo-text { font-family: 'Playfair Display', serif; font-size: 2rem; font-weight: 900; letter-spacing: -0.05em; margin-bottom: 0.5rem; color: var(--ink); }
  .internship-card-main .company-subtitle { font-family: 'DM Mono', monospace; font-size: 0.62rem; letter-spacing: 0.2em; color: var(--muted); text-transform: uppercase; margin-bottom: 2.5rem; }
  .internship-card-main .intern-divider { width: 100%; height: 1px; background: var(--border); margin-bottom: 2rem; }
  .internship-card-main .intern-role { font-family: 'Playfair Display', serif; font-size: 1.2rem; font-weight: 700; margin-bottom: 0.25rem; }
  .internship-card-main .intern-period { font-family: 'DM Mono', monospace; font-size: 0.65rem; color: var(--muted); letter-spacing: 0.1em; margin-bottom: 2rem; }
  .internship-card-main .intern-stamp { display: inline-block; padding: 0.5rem 1.2rem; border: 2px solid var(--accent); font-family: 'DM Mono', monospace; font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase; color: var(--accent); transform: rotate(-2deg); }
  .internship-highlights { margin-top: 2rem; display: flex; flex-direction: column; gap: 1.5rem; }
  .highlight-item { display: flex; gap: 1.5rem; align-items: flex-start; padding-bottom: 1.5rem; border-bottom: 1px solid var(--border); }
  .highlight-icon { flex-shrink: 0; width: 40px; height: 40px; background: var(--ink); color: var(--gold); display: flex; align-items: center; justify-content: center; font-size: 1.1rem; }
  .highlight-text strong { display: block; font-family: 'Playfair Display', serif; font-size: 1.05rem; font-weight: 700; margin-bottom: 0.2rem; }
  .highlight-text span { font-size: 0.9rem; color: var(--muted); font-weight: 300; }
  .contact { background: var(--ink); color: var(--paper); text-align: center; padding: 10rem 4rem; position: relative; overflow: hidden; }
  .contact::before { content: 'HELLO'; position: absolute; font-family: 'Playfair Display', serif; font-size: 22vw; font-weight: 900; color: rgba(255,255,255,0.02); top: 50%; left: 50%; transform: translate(-50%, -50%); white-space: nowrap; pointer-events: none; }
  .contact .section-eyebrow { justify-content: center; color: var(--gold); }
  .contact .section-eyebrow::after { background: var(--gold); }
  .contact .section-title { color: var(--paper); margin-bottom: 2rem; }
  .contact-sub { font-size: 1.1rem; color: rgba(245,240,232,0.5); font-weight: 300; margin-bottom: 4rem; font-style: italic; }
  .contact-links { display: flex; justify-content: center; gap: 3rem; flex-wrap: wrap; margin-bottom: 5rem; }
  .contact-link { display: flex; flex-direction: column; align-items: center; gap: 0.75rem; text-decoration: none; color: var(--paper); transition: transform 0.3s; }
  .contact-link:hover { transform: translateY(-6px); }
  .contact-link .link-icon { width: 56px; height: 56px; border: 1px solid rgba(245,240,232,0.2); display: flex; align-items: center; justify-content: center; font-size: 1.3rem; transition: background 0.3s, border-color 0.3s; }
  .contact-link:hover .link-icon { background: var(--gold); border-color: var(--gold); color: var(--ink); }
  .contact-link .link-label { font-family: 'DM Mono', monospace; font-size: 0.62rem; letter-spacing: 0.2em; text-transform: uppercase; color: rgba(245,240,232,0.4); }
  .contact-cta { display: inline-block; padding: 1.2rem 3.5rem; background: var(--gold); color: var(--ink); font-family: 'DM Mono', monospace; font-size: 0.75rem; letter-spacing: 0.2em; text-transform: uppercase; text-decoration: none; transition: transform 0.3s, box-shadow 0.3s; }
  .contact-cta:hover { transform: translateY(-3px); box-shadow: 0 12px 40px rgba(184,151,58,0.3); }
  footer { background: var(--ink); border-top: 1px solid rgba(255,255,255,0.06); padding: 3rem 4rem; display: flex; justify-content: space-between; align-items: center; color: rgba(245,240,232,0.3); }
  footer .footer-name { font-family: 'Playfair Display', serif; font-size: 1.1rem; color: rgba(245,240,232,